const prompt = require('prompt-sync')()

// dizer se o numero é positivo ou negativo

let mumero = Number (prompt(`digite u numero`))
if(numero > 0){
    console.log(`o ${nmr} é positivo`)
}else{
    console.log(`o ${nmr} é negativo`)
}